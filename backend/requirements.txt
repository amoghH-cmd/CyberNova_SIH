fastapi>=0.111.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.0
alembic>=1.13.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
pandas>=2.0.0
scikit-learn>=1.4.0
numpy>=1.26.0
joblib>=1.3.0
websockets>=12.0
python-multipart>=0.0.9
google-generativeai>=0.5.0

# Deepfake detection (Community Forensics ViT model)
# torch/torchvision are installed separately in render.yaml's buildCommand
# from the CPU-only wheel index, to avoid pulling the multi-GB CUDA build
# on a CPU-only instance.
timm>=1.0.0
huggingface-hub>=0.20.0
opencv-python-headless==4.14.0.94
pillow>=10.0.0

# AI decision layer (SOAR triage + Copilot)
google-genai>=0.3.0
